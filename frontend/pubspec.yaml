name: streamgrab
description: A premium video downloader for iOS and Windows.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  url_launcher: ^6.1.11
  google_fonts: ^6.1.0
  flutter_animate: ^4.2.0
  glassmorphism_ui: ^0.3.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
